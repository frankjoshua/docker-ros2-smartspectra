# Set which metric groups the node computes (real node) or simulates (fake node).
# Valid group names: "breathing", "cardio", "eda", "face".
# Unknown names are ignored with a warning; an empty array selects the SDK default.
string[] metrics

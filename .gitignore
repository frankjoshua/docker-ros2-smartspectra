# colcon build artifacts (repo root is the workspace)
build/
install/
log/

# secrets — never commit (template lives in .env.example)
.env

# not source: agent canvas UI state + crash dumps
.canvas/
core
